import type { ImageConfig, ImageSource } from "./types";

/**
 * 画像のHTMLをレンダリング
 */
export function renderImage(
	_source: ImageSource | ImageSource[],
	_config?: Partial<ImageConfig>,
): string {
	// TODO: イテレーション2で実装
	return "";
}
