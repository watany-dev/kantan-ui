# E2Eテストカバレッジ分析

## 現状サマリ

- **テストファイル数**: 6
- **テストケース数**: 36
- **テストフレームワーク**: Playwright
- **対応ブラウザ**: Chromiumのみ

## 現在カバーされている機能

### WebSocket通信とパッチ適用 (websocket.spec.ts - 17 tests)
- [x] WebSocket接続の確立
- [x] 初期HTML読み込み
- [x] イベント送信（ボタンクリック）
- [x] replaceRootパッチによるUI更新
- [x] カウンター更新（インクリメント、デクリメント、リセット）
- [x] スライダー値更新
- [x] テキスト入力値更新
- [x] セレクトボックス値更新
- [x] セッション状態の永続化
- [x] 連続更新対応（複数高速クリック）
- [x] DOM整合性検証
- [x] 複数ウィジェット同時操作

### セッション管理

**タブスコープ (session-scope.spec.ts - 3 tests)**
- [x] localStorageを用いたセッション管理
- [x] タブごとの独立セッション
- [x] タブスコープではCookieが設定されないことの確認

**ブラウザスコープ (session-scope-browser.spec.ts - 5 tests)**
- [x] HttpOnlyクッキーの設定確認
- [x] ページリロード後のセッション永続化
- [x] 複数タブでのセッション共有
- [x] JavaScriptからのセッションIDアクセス制限
- [x] タブ間での状態共有

### フォーカス・UI状態保持 (focus-preservation.spec.ts - 8 tests)
- [x] ボタンクリック後のDOM正確性
- [x] テキスト入力時のフォーカス保持
- [x] replaceNodeによる要素ID保持
- [x] スクロール位置の保持（ボタンクリック後）
- [x] スクロール位置の保持（スライダー変更後）
- [x] テキスト選択範囲の保持
- [x] カーソル位置の保持
- [x] 複数パッチ必要な場合のreplaceRoot処理
- [ ] スライダーのフォーカス保持 (`test.fixme()`)

### ストリーミング機能 (streaming.spec.ts - 5 tests)
- [x] ストリーミング有効時の初期ページ読み込み
- [x] カウンター表示
- [x] ボタンクリックによるカウンター更新
- [x] ストリーミングアイテムの完全表示
- [x] 複数ボタンクリックの処理

### 基本テスト (example.spec.ts - 1 test)
- [x] ホームページの表示確認

---

## 不足している観点

### 1. 出力API（`kt.*`）のテスト - 優先度: 高

| API | 状況 | 備考 |
|-----|------|------|
| `kt.write()` | 未テスト | テキスト/HTML出力 |
| `kt.title()` | 未テスト | タイトル出力 |
| `kt.header()` | 未テスト | ヘッダー出力 |
| `kt.subheader()` | 未テスト | サブヘッダー出力 |
| `kt.text()` | 未テスト | テキスト出力 |
| `kt.divider()` | 未テスト | 区切り線出力 |
| `kt.html()` | 未テスト | 生HTML出力、XSS対策の検証も必要 |

### 2. エラーハンドリング・回復系 - 優先度: 高

- [ ] WebSocket切断時の再接続動作
- [ ] サーバー応答タイムアウト時の挙動
- [ ] ネットワーク一時切断からの復帰
- [ ] 不正なパッチ受信時のエラーハンドリング
- [ ] セッション期限切れ時の挙動

### 3. 差分パッチ処理 - 優先度: 高

- [x] `replaceRoot` - テスト済み
- [x] `replaceNode` - テスト済み
- [ ] `removeNode` - 未テスト
- [ ] `insertNode` - 未テスト
- [ ] 複合パッチ（複数操作同時）

### 4. セキュリティ - 優先度: 中

- [ ] `kt.html()`でのXSS対策確認
- [ ] `kt.text()`でのHTMLエスケープ確認
- [ ] CSP（Content Security Policy）の動作確認
- [ ] 不正なセッションIDでのアクセス拒否

### 5. エッジケース - 優先度: 中

- [ ] 大量データ（長いリスト、大きなDOM）でのパフォーマンス
- [ ] 超高速連打時の競合状態
- [ ] 並行する複数のWebSocket更新
- [ ] 空の値・境界値（スライダーmin/max、空文字列入力など）
- [ ] 極端に長い文字列入力

### 6. キーボード操作 - 優先度: 中

- [ ] Tabキーによるフォーカス移動
- [ ] Enterキーでのボタン押下
- [ ] スライダーの矢印キー操作
- [ ] セレクトボックスのキーボード選択

### 7. ウィジェットの詳細動作 - 優先度: 中

| ウィジェット | カバー状況 | 不足 |
|-------------|-----------|------|
| button | 基本動作のみ | disabled状態、連打制御 |
| slider | 基本動作のみ | フォーカス保持(fixme)、ステップ値 |
| text_input | 基本動作のみ | IME入力、ペースト、maxlength |
| selectbox | 基本動作のみ | キーボード選択、disabled options |

### 8. ブラウザ互換性 - 優先度: 低

- [x] Chromium
- [ ] Firefox
- [ ] WebKit (Safari)

### 9. アクセシビリティ - 優先度: 低

- [ ] ARIA属性の適切な設定
- [ ] スクリーンリーダー対応
- [ ] コントラスト比
- [ ] フォーカスインジケーター

### 10. 設定オプション - 優先度: 低

- [ ] `client.maxReconnectAttempts` の動作確認
- [ ] `client.baseReconnectDelay` の動作確認
- [ ] カスタム `sessionKey` の動作

### 11. 複数タブ/ウィンドウ - 優先度: 低

- [x] ブラウザスコープでの状態共有
- [ ] タブ間でのリアルタイム同期
- [ ] 片方のタブを閉じた際の挙動

---

## 対応方針

### フェーズ1（高優先度）
1. 出力APIのテスト追加
2. `removeNode`/`insertNode`パッチのテスト
3. WebSocket再接続のテスト

### フェーズ2（中優先度）
4. セキュリティ関連テスト
5. エッジケーステスト
6. キーボード操作テスト

### フェーズ3（低優先度）
7. ブラウザ互換性
8. アクセシビリティ
9. 設定オプション網羅

---

## 更新履歴

- 2026-01-04: 初版作成
